let adminToken=localStorage.getItem('adminToken')||'';
async function api(action,payload={}){const res=await fetch(API_URL,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action,storeId:STORE_ID,adminToken,...payload})});return res.json();}
function showAdmin(){document.getElementById('loginView').style.display='none';document.getElementById('adminView').style.display='grid'}
async function login(){const res=await api('adminLogin',{username:username.value,password:password.value});if(res.success){adminToken=res.token;localStorage.setItem('adminToken',adminToken);showAdmin();loadDashboard()}else alert(res.message||'เข้าสู่ระบบไม่สำเร็จ')}
document.getElementById('loginBtn').onclick=login;if(adminToken){showAdmin();loadDashboard()}
async function loadDashboard(){const data=await api('adminDashboard');document.getElementById('statBookings').textContent=data.todayBookings||0;document.getElementById('statPending').textContent=data.pendingBookings||0;document.getElementById('statRevenue').textContent=(data.revenue||0).toLocaleString('th-TH');const tbody=document.getElementById('bookingRows');tbody.innerHTML='';(data.bookings||[]).forEach(b=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${b.BookingID}</td><td>${b.CustomerName}</td><td>${b.TableID}</td><td>${b.BookingDate} ${b.BookingTime}</td><td>${b.Status}</td><td><button onclick="updateBooking('${b.BookingID}','confirmed')">Confirm</button></td>`;tbody.appendChild(tr)})}
async function updateBooking(id,status){const res=await api('updateBookingStatus',{bookingId:id,status});alert(res.message||'updated');loadDashboard()}
